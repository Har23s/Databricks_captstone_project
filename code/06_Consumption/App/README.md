1. Create the streamlit data app in the databricks compute section
2. Once the stream-lit app is ceated click on the deploment link open the app.py to code the app , requiremet text to add the modules and the framework and the yaml to add the cluster id or the DWH serverless warehouse id .

